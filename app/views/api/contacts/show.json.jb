{
  id: @contact.id,
  first_name: @contact.first_name,
  middle_name: @contact.middle_name,
  last_name: @contact.last_name,
  full_name: @contact.full_name,
  bio: @contact.bio,
  email: @contact.email,
  phone_number: @contact.japanese_phone_number,
  created_at: @contact.friendly_created_at
}
